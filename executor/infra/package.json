{
  "name": "executor-infra",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "diff": "sst diff",
    "refresh": "sst refresh",
    "typecheck": "bunx --package @typescript/native-preview tsgo --noEmit -p tsconfig.json",
    "typecheck:slow": "bunx tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf node_modules .turbo"
  },
  "dependencies": {
    "@pulumi/command": "^1.1.3",
    "@pulumi/pulumi": "^3.220.0",
    "@pulumiverse/vercel": "^4.5.0",
    "pulumi-stripe": "^0.0.26",
    "sst": "^3.17.38"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "typescript": "^5.9.3"
  }
}
