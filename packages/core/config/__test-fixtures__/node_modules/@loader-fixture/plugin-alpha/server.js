// Hand-rolled mimic of a `definePlugin(...)`-produced factory: a function
// that takes options and returns a plugin-shaped object. Captures the
// options it received so loader tests can assert merging behaviour.
export default (options) => ({
  id: "alpha",
  packageName: "@loader-fixture/plugin-alpha",
  __optionsReceived: options ?? null,
});
