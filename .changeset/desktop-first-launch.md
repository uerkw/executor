---
"executor": patch
---

Ship the Executor Desktop app — signed + notarized on macOS, AppImage/deb/rpm for Linux, `.exe` for Windows. State lives at `~/.executor/`, shared with the CLI. See release notes for the first-launch experience.
